apiVersion: v1
kind: Service
metadata:
  name: openbao-node-port
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: openbao
  ports:
    - port: 8200
      targetPort: 8200
      nodePort: 30200
